services:
  postgres:
    image: postgres:16
    environment:
      POSTGRES_USER: ouroloop
      POSTGRES_PASSWORD: ouroloop
      POSTGRES_DB: ouroloop
    ports:
      - '5432:5432'
    volumes:
      - pgdata:/var/lib/postgresql/data
volumes:
  pgdata:
